# Web interface

React application for both the event player flow and protected administration interface.

```sh
npm install
npm run dev
```

The development server proxies API and health requests to `http://localhost:8080`.

Quality checks:

```sh
npm run lint
npm run test
npm run build
```
